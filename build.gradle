// load the plugin via the Maval Local repository
buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath "com.kazurayam:visualtestinginks-gradle-plugin:0.0.0"
    }
}

// or you can load the plugin via the Gradle Plugin Portal https://plugins.gradle.org/ once registred
//plugins {
//    id "com.github.kazurayam.visualtestinginks-gradle-plugin" version="0.0.0"
//}




apply plugin: 'com.github.kazurayam.visualtestinginks-gradle-plugin'

// $ ./gradlew export
// $ ./gradlew import