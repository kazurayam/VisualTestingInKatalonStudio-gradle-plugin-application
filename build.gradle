buildscript {
    ext {
        visualtestinginksPluginVersion = "0.0.0"
    }
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath "com.kazurayam:VisualTestingInKatalonStudio-gradle-plugin:${visualtestinginksPluginVersion}"
    }
}

/*
// via the Gradle Plugin Portal https://plugins.gradle.org/
plugins {
    id "com.github.kazurayam.VisualTestingInKatalonStudio-gradle-plugin" version="0.0.0"
}
 */
 
apply plugin: 'com.github.kazurayam.VisualTestingInKatalonStudio-gradle-plugin'

// $ ./gradlew export
// $ ./gradlew import